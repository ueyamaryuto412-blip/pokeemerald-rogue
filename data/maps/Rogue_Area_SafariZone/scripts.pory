mapscripts Rogue_Area_SafariZone_MapScripts {}

script Rogue_Area_SafariZone_TalkSign
{
    lock

    msgbox(format("{JPN}サファリエリアで さいきん みかけた ポケモンの いちらんだ!"))
    
    begindynamicmultichoice
    appenddynamicmultichoice("{JPN}ポケモンを みる", 0)
    appenddynamicmultichoice("{JPN}ポケモンを かえす", 1)
    appenddynamicmultichoice("{JPN}やめる", MULTI_B_PRESSED)

    message(format("{JPN}なにを しますか?"))
    waitmessage
    enddynamicmultichoice(1, 0, 6, TRUE)

    switch(var(VAR_RESULT))
    {
        case 0:
            special(RogueSafari_CompactEmptyEntries)
            special(Special_SelectSafariMonInPokedex)
            waitstate

        case 1:
            msgbox(format("{JPN}サファリの ポケモンを ぜんぶ かえしますか?"), MSGBOX_YESNO)

            if(var(VAR_RESULT) == YES)
            {
                special(RogueSafari_ResetAllRegularEntries)
                special(ReloadWarpSilent)
            }
    }

    release
}